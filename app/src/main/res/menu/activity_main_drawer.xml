<?xml version="1.0" encoding="utf-8"?>
<menu xmlns:android="http://schemas.android.com/apk/res/android">



    <item android:title="Activities">
        <menu>
            <item
                android:id="@+id/nav_cycling"
                android:icon="@drawable/ic_cycling"
                android:title="Cycling" />
            <item
                android:id="@+id/nav_running"
                android:icon="@drawable/ic_running"
                android:title="Running" />
            <item
                android:id="@+id/nav_crossfit"
                android:icon="@drawable/ic_crossfit"
                android:title="Crossfit" />
        </menu>
    </item>

    <group android:checkableBehavior="single">
        <item
            android:id="@+id/nav_settings"
            android:icon="@drawable/ic_settings"
            android:title="Settings" />
        <item
            android:id="@+id/nav_logout"
            android:icon="@drawable/ic_logout"
            android:title="Logout" />

    </group>

</menu>
